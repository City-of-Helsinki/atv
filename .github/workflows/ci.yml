name: CI
on:
  push:
    branches: [main, release]
  pull_request:
jobs:
  common-ci:
    uses: City-of-Helsinki/.github/.github/workflows/ci-django-api.yml@ci-django-api.yml
    secrets: inherit
    # with:
    #   python-version: 3.11
